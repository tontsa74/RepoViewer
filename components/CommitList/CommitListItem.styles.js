import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#fff',
        borderWidth: 1,
        flexDirection: 'row',
    },
    avatar: {
      backgroundColor: '#ddd',
      height: 40,
      width: 40,
    },
    itemTexts: {
        fontSize: 16,
        paddingHorizontal: 2,
    },
});

export default styles;
