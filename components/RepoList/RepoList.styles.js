import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#eee',
        padding: 10,
    },
    listHeader: {
        fontWeight: 'bold',
        fontSize: 20,
        padding: 10,
    },
});

export default styles;
